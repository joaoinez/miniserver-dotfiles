---
services:
  hermes:
    image: nousresearch/hermes-agent:main@sha256:75298058a7960bcafb94e97549a1061bdb0bb0cd1cd0570d4c685247eb28cb5d
    container_name: hermes
    restart: unless-stopped
    network_mode: service:tailscale-hermes
    command: gateway run
    # ports:
    #   - "8642:8642"
    volumes:
      - ~/.hermes:/opt/data
    environment:
      - HERMES_DASHBOARD=1
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
    # security_opt:
    #   - no-new-privileges:true
    # cap_drop:
    #   - ALL
    # cap_add:
    #   - CHOWN
    #   - SETUID
    #   - SETGID
    depends_on:
      - tailscale-hermes

  tailscale-hermes:
    image: tailscale/tailscale:v1.98.3
    container_name: ts-hermes
    hostname: hermes
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY:?TS_AUTHKEY must be set}
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
      - TS_SERVE_CONFIG=/config/ts-serve-config.json
    volumes:
      - tailscale-state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
      - ./:/config:ro
    cap_add:
      - NET_ADMIN
    restart: unless-stopped

volumes:
  tailscale-state:
